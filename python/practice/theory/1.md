# Python: Краткая теория (Дни 1-3)

## 📊 День 1: Переменные и типы данных

### Основные типы данных
```python
# Числовые типы
age = 25                    # int
price = 99.99              # float
complex_num = 3 + 4j       # complex

# Строки
name = "Алекс"             # str
description = 'Описание'   # str (одинарные кавычки)
multiline = """
Многострочный
текст
"""                        # str (тройные кавычки)

# Булевы значения
is_active = True           # bool
is_deleted = False         # bool

# Специальное значение
data = None                # NoneType
```

### Современные возможности Python 3.12+ (2025)
```python
# Type hints (аннотации типов)
def greet(name: str, age: int) -> str:
    return f"Привет, {name}! Тебе {age} лет."

# Walrus operator (Python 3.8+)
if (length := len(input_data)) > 10:
    print(f"Длинные данные: {length} символов")

# Улучшенные f-strings (Python 3.12+)
value = 42
print(f"{value = }")  # Выведет: value = 42
```

### Операторы
```python
# Арифметические
a + b      # сложение
a - b      # вычитание
a * b      # умножение
a / b      # деление (float)
a // b     # целочисленное деление
a % b      # остаток от деления
a ** b     # возведение в степень

# Сравнения
a == b     # равно
a != b     # не равно
a < b      # меньше
a <= b     # меньше или равно
a > b      # больше
a >= b     # больше или равно

# Логические
and, or, not
is, is not  # сравнение объектов (identity)
in, not in  # принадлежность

# Присваивания
x = 5      # обычное
x += 1     # x = x + 1
x *= 2     # x = x * 2
```

## 🔄 Ввод/вывод и условные операторы

### Ввод и вывод
```python
# Ввод (всегда возвращает строку)
name = input("Введите имя: ")
age = int(input("Введите возраст: "))

# Вывод
print("Привет!")
print("Имя:", name, "Возраст:", age)
print(f"Привет, {name}! Тебе {age} лет.")  # f-strings

# Форматирование чисел
pi = 3.14159
print(f"π ≈ {pi:.2f}")  # π ≈ 3.14
```

### Условные операторы
```python
# Базовый if
if age >= 18:
    print("Совершеннолетний")
elif age >= 14:
    print("Подросток")
else:
    print("Ребёнок")

# Match-case (Python 3.10+) - современная альтернатива if-elif
grade = "A"
match grade:
    case "A":
        print("Отлично!")
    case "B":
        print("Хорошо")
    case "C" | "D":  # Множественные варианты
        print("Удовлетворительно")
    case _:  # default case
        print("Неизвестная оценка")

# Тернарный оператор
status = "взрослый" if age >= 18 else "ребёнок"
```

## 🔁 День 2: Циклы

### Цикл for
```python
# По последовательности
for i in range(5):          # 0, 1, 2, 3, 4
    print(i)

for i in range(1, 6):       # 1, 2, 3, 4, 5
    print(i)

for i in range(0, 10, 2):   # 0, 2, 4, 6, 8
    print(i)

# По строке
for char in "Python":
    print(char)

# С индексом
for i, char in enumerate("Python"):
    print(f"{i}: {char}")

# По списку
numbers = [1, 2, 3, 4, 5]
for num in numbers:
    print(num)
```

### Цикл while
```python
count = 0
while count < 5:
    print(count)
    count += 1

# Бесконечный цикл с условием выхода
while True:
    user_input = input("Введите 'exit' для выхода: ")
    if user_input == "exit":
        break
    print(f"Вы ввели: {user_input}")
```

### Управление циклами
```python
for i in range(10):
    if i == 3:
        continue    # пропустить итерацию
    if i == 7:
        break      # выйти из цикла
    print(i)

# Цикл с else (выполняется, если цикл завершился без break)
for i in range(5):
    if i == 10:  # никогда не выполнится
        break
else:
    print("Цикл завершён нормально")
```

## 🎯 Функции

### Определение и вызов
```python
# Простая функция
def greet():
    print("Привет!")

greet()

# С параметрами
def add(a, b):
    return a + b

result = add(5, 3)

# С значениями по умолчанию
def greet(name, greeting="Привет"):
    return f"{greeting}, {name}!"

print(greet("Анна"))              # Привет, Анна!
print(greet("Боб", "Здравствуй")) # Здравствуй, Боб!
```

### Современные возможности (2025)
```python
# Type hints
def calculate_area(radius: float) -> float:
    return 3.14159 * radius ** 2

# Позиционные и именованные аргументы
def create_user(name: str, /, age: int, *, email: str) -> dict:
    # name - только позиционный
    # age - может быть позиционным или именованным
    # email - только именованный
    return {"name": name, "age": age, "email": email}

user = create_user("Анна", age=25, email="anna@example.com")
```

### Переменное количество аргументов
```python
# *args - произвольное количество позиционных аргументов
def sum_all(*args):
    return sum(args)

print(sum_all(1, 2, 3, 4))  # 10

# **kwargs - произвольное количество именованных аргументов
def print_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_info(name="Анна", age=25, city="Москва")
```

### Lambda функции
```python
# Анонимные функции
square = lambda x: x ** 2
print(square(5))  # 25

# Часто используются с map, filter
numbers = [1, 2, 3, 4, 5]
squares = list(map(lambda x: x ** 2, numbers))
evens = list(filter(lambda x: x % 2 == 0, numbers))
```

## 📋 День 3: Структуры данных

### Списки (list)
```python
# Создание
numbers = [1, 2, 3, 4, 5]
mixed = [1, "hello", 3.14, True]
empty = []

# Основные операции
numbers.append(6)           # добавить в конец
numbers.insert(0, 0)        # вставить по индексу
numbers.remove(3)           # удалить по значению
popped = numbers.pop()      # удалить и вернуть последний
numbers.sort()              # сортировка на месте
numbers.reverse()           # реверс на месте

# Срезы (slicing)
nums = [0, 1, 2, 3, 4, 5]
print(nums[1:4])    # [1, 2, 3]
print(nums[:3])     # [0, 1, 2]
print(nums[2:])     # [2, 3, 4, 5]
print(nums[::2])    # [0, 2, 4] (каждый второй)
print(nums[::-1])   # [5, 4, 3, 2, 1, 0] (реверс)

# List comprehensions (современный стиль)
squares = [x**2 for x in range(10)]
evens = [x for x in range(20) if x % 2 == 0]
matrix = [[i*j for j in range(3)] for i in range(3)]
```

### Кортежи (tuple)
```python
# Создание
point = (10, 20)
colors = ("red", "green", "blue")
single = (42,)  # обязательна запятая для одного элемента

# Распаковка
x, y = point
first, *middle, last = (1, 2, 3, 4, 5)  # first=1, middle=[2,3,4], last=5

# Named tuples (современный подход)
from collections import namedtuple

Person = namedtuple('Person', ['name', 'age', 'email'])
user = Person("Анна", 25, "anna@example.com")
print(user.name)  # Анна

# Dataclasses (Python 3.7+, рекомендуется в 2025)
from dataclasses import dataclass

@dataclass
class Point:
    x: float
    y: float
    
    def distance_from_origin(self) -> float:
        return (self.x ** 2 + self.y ** 2) ** 0.5

p = Point(3.0, 4.0)
print(p.distance_from_origin())  # 5.0
```

### Множества (set)
```python
# Создание
fruits = {"apple", "banana", "orange"}
numbers = set([1, 2, 2, 3, 3, 3])  # {1, 2, 3}
empty_set = set()  # НЕ {} - это словарь!

# Операции
fruits.add("grape")
fruits.remove("banana")     # KeyError если нет элемента
fruits.discard("kiwi")      # без ошибки если нет элемента

# Множественные операции
set1 = {1, 2, 3, 4}
set2 = {3, 4, 5, 6}

union = set1 | set2         # {1, 2, 3, 4, 5, 6}
intersection = set1 & set2  # {3, 4}
difference = set1 - set2    # {1, 2}
symmetric_diff = set1 ^ set2 # {1, 2, 5, 6}

# Set comprehensions
even_squares = {x**2 for x in range(10) if x % 2 == 0}
```

### Словари (dict)
```python
# Создание
person = {"name": "Анна", "age": 25, "city": "Москва"}
empty_dict = {}

# Доступ и изменение
print(person["name"])           # "Анна"
print(person.get("email", ""))  # "" (если ключа нет)
person["email"] = "anna@example.com"
person.update({"phone": "+7123456789", "age": 26})

# Удаление
del person["city"]
email = person.pop("email")     # удалить и вернуть значение

# Итерация
for key in person:              # по ключам
    print(key, person[key])

for key, value in person.items():  # по парам ключ-значение
    print(f"{key}: {value}")

# Dict comprehensions
squares = {x: x**2 for x in range(5)}  # {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}

# Современные возможности (Python 3.9+)
dict1 = {"a": 1, "b": 2}
dict2 = {"c": 3, "d": 4}
merged = dict1 | dict2  # объединение словарей
```

## 🚀 Современные возможности Python (2025)

### Pattern Matching с структурами данных
```python
def process_data(data):
    match data:
        case {"type": "user", "name": str(name)}:
            return f"Пользователь: {name}"
        case {"type": "product", "price": float(price)} if price > 100:
            return f"Дорогой товар: {price}"
        case [first, *rest] if len(rest) > 0:
            return f"Список: первый={first}, остальные={len(rest)}"
        case _:
            return "Неизвестный формат данных"
```

### Улучшенная обработка ошибок
```python
# Улучшенные сообщения об ошибках (Python 3.10+)
try:
    result = data["users"][0]["profile"]["email"]
except KeyError as e:
    print(f"Отсутствует ключ: {e}")
    # Python точно покажет, какой именно ключ отсутствует
```

### Производительность и типизация
```python
# Типизация для лучшей читаемости и IDE поддержки
from typing import List, Dict, Optional, Union

def process_users(users: List[Dict[str, Union[str, int]]]) -> Optional[str]:
    """Обрабатывает список пользователей."""
    if not users:
        return None
    return f"Обработано {len(users)} пользователей"

# Generic типы (Python 3.12+)
from typing import Generic, TypeVar

T = TypeVar('T')

class Stack(Generic[T]):
    def __init__(self) -> None:
        self._items: List[T] = []
    
    def push(self, item: T) -> None:
        self._items.append(item)
    
    def pop(self) -> T:
        return self._items.pop()
```

## 💡 Лучшие практики (2025)

### Читаемость кода
```python
# Хорошо: описательные имена
user_age = 25
is_valid_email = True
calculate_total_price()

# Плохо: сокращения
usr_age = 25
is_val = True
calc_tot_pr()
```

### Использование современных конструкций
```python
# Современно (2025)
users = [
    {"name": "Анна", "age": 25},
    {"name": "Боб", "age": 30}
]

# List comprehension с условием
adults = [user for user in users if user["age"] >= 18]

# Dict comprehension
ages_by_name = {user["name"]: user["age"] for user in users}

# Walrus operator для оптимизации
if (user_count := len(users)) > 10:
    print(f"Много пользователей: {user_count}")
```

### Обработка данных
```python
# Эффективная работа с большими данными
from itertools import islice

# Генераторы вместо списков для экономии памяти
def fibonacci_generator():
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b

# Получить первые 10 чисел Фибоначчи
first_10_fib = list(islice(fibonacci_generator(), 10))
```

Эта теория покрывает все основные концепции из тренажеров и включает современные возможности Python актуальные на 2025 год!