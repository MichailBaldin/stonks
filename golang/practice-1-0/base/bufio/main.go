package main

// 1. Создай `bufio.Reader` из `strings.NewReader("hello\nworld\n")`,
// считай строку через `.ReadString('\n')` и выведи.

// 2. Используй `bufio.Reader.ReadBytes('\n')` для чтения первой строки
// из `strings.NewReader("one\ntwo\n")`, выведи срез байт.

// 3. Считай первые 3 байта из строки `"abcdef"` с помощью `.Read([]byte)`
// через `bufio.Reader`.

// 4. Используй `r.Peek(4)` из `bufio.Reader`, чтобы заглянуть в первые 4 байта строки `"sample"`
// без удаления из буфера. Затем прочитай их обычным `Read`.

// 5. Используй `r.ReadRune()` для построчного чтения рун из строки `"Привет"`,
// выведи каждую руну и её размер.

// 6. Создай `bufio.Reader`, затем вызови `.UnreadByte()` после чтения одного байта
// и снова считай — покажи, что байт не пропал.

// 7. То же самое, но с `.UnreadRune()` — считай руну и верни её обратно в поток,
// затем снова прочитай.

// 8. Используя `bufio.Writer`, создай буфер и запиши туда строку `"buffered"`,
// не вызывай `.Flush()` и покажи, что данные не попадают в целевой буфер до вызова `Flush`.

// 9. Запиши в `bufio.Writer` строку `"flushed!"`, затем вызови `.Flush()`
// и выведи результат из внутреннего `bytes.Buffer`.

// 10. Используй `.WriteRune('★')` через `bufio.Writer`, вызови `.Flush()`,
// выведи содержимое.

// 11. Используй `.WriteByte('X')` несколько раз, затем `.Flush()`,
// и проверь содержимое буфера.

// 12. Прочитай строку `"hello\nworld\n"` построчно с помощью `bufio.Scanner`,
// выведи каждую строку.

// 13. Используй `bufio.Scanner` для чтения слов (а не строк) из строки `"one two three"`,
// установив `.Split(bufio.ScanWords)`.

// 14. Используй `bufio.Scanner` для чтения по рунaм из строки `"Go!"`
// с помощью `.Split(bufio.ScanRunes)`.

// 15. Используй `bufio.Scanner` и `.Split(bufio.ScanLines)` (по умолчанию) —
// выведи каждую строку отдельно из `"line1\nline2\n"`.

// 16. Прочитай первые 5 байт из строки `"abcdefgh"` с помощью `bufio.Reader.Discard(5)`,
// затем прочитай остаток и выведи.

// 17. Используй `bufio.Reader.Size()` и `Buffered()` чтобы узнать:
// - размер внутреннего буфера;
// - сколько байт там сейчас.

// 18. Создай `bufio.NewReaderSize` и `bufio.NewWriterSize` с размером 4,
// покажи поведение при записи 10 байт без `Flush()`.

// 19. Используй `bufio.Scanner` и вызови `.Scan()` в цикле,
// затем после завершения проверь `scanner.Err()` на ошибку.

// 20. Реализуй свою функцию `ReadLine(r *bufio.Reader) (string, error)`,
// которая вручную считывает байты до `\n` и возвращает строку.
