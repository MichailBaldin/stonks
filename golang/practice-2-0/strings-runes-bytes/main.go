package main

// ======================================
// Тренажёр: strings, runes, bytes (v1)
// ======================================
// Правила: не полагайся на "байт == символ". Строки иммутабельны. UTF-8 по умолчанию.

// --- Блок A. База строк и байтов ---

// 1. Создай строку s := "Go" и выведи len(s) и s[0]. Чем является s[0]?
// 2. Создай строку с не-ASCII: s := "привет". Выведи len(s) и количество рун (utf8.RuneCountInString).
// 3. Попробуй изменить символ по индексу: s[0] = 'X' — зафиксируй ошибку компиляции (строка иммутабельна).
// 4. Преобразуй "привет" в []byte и []rune. Выведи длины обоих массивов.
// 5. Собери строку обратно из []rune и из []byte. Сравни (==) с исходной.
// 6. Создай []byte с невалидной UTF-8 последовательностью; проверь utf8.Valid и utf8.ValidString.
// 7. Выведи количество байт в символе 'Я' через utf8.RuneLen('Я') и в ASCII 'A'.
// 8. Возьми подстроку по байтам: s := "мир"; s[:2] — что получится? Объясни, почему ломается.
// 9. Сделай безопасное взятие подстроки по рунам: первые 2 руны строки "мир".
// 10. Сравни строку с байтовым срезом: bytes.Equal([]byte(s), b) vs string(b) == s.

// --- Блок B. Итерации и декодирование UTF-8 ---

// 11. Пройди строку range’ом и выведи индекс и руну (индекс — в байтах).
// 12. То же, но вручную через utf8.DecodeRuneInString с явным шагом по байтам.
// 13. Декодируй руны из []byte через utf8.DecodeRune, учитывая utf8.RuneError.
// 14. Подсчитай частоты рун в строке сэмпла (map[rune]int).
// 15. Подсчитай частоты БАЙТ в той же строке (map[byte]int). Сравни распределение.
// 16. Реализуй поиск первого байтового индекса конкретной руны в строке (без strings.IndexRune).
// 17. Реализуй обратную итерацию по рунам (DecodeLastRuneInString).
// 18. Проверь, что \n — одна руна, а "👋" — несколько байт (и одна руна).
// 19. Сконструируй строку с комбинируемым акцентом: "é" (e + U+0301). Посчитай руны и байты.
// 20. Сравни "é" (U+00E9) и "e"+"́" по EqualFold и по ==. Обсуди нормализацию (в комментарии).

// --- Блок C. Поиск, сравнение, префиксы/суффиксы ---

// 21. Используй strings.Contains / HasPrefix / HasSuffix для ASCII и не-ASCII примеров.
// 22. Найди индекс подстроки через strings.Index / LastIndex и для руны через strings.IndexRune.
// 23. Сравни строки лексикографически через strings.Compare и через операторы <, >.
// 24. Сделай регистронезависимое сравнение EqualFold на "straße" и "STRASSE".
// 25. Реализуй ContainsFold(s, sub) через приведение к одному регистру и покажи, почему EqualFold предпочтительнее для сравнения.

// --- Блок D. Разбиение, склейка, тримминг ---

// 26. Разбей строку по пробелам через strings.Fields и по запятой через strings.Split.
// 27. Сделай SplitN и Join обратно, сохраняй порядок.
// 28. Удали пробелы по краям: TrimSpace; затем Trim для конкретных рун.
// 29. Используй TrimFunc с unicode.IsLetter для обрезки не-буквенных символов.
// 30. Разбей строку на слова с кастомным FieldsFunc: слово — последовательность unicode.IsLetter.

// --- Блок E. Замены и маппинг ---

// 31. Заменяй все вхождения strings.ReplaceAll, и только первые N — strings.Replace.
// 32. Используй strings.Map для удаления всех цифр из строки.
// 33. Преобразуй каждый символ в ROT13 через strings.Map (только латиница).
// 34. Преврати snake_case в kebab-case и обратно (простые замены).
// 35. Нормализуй пробелы: несколько пробелов -> один, табы тоже считаем пробелами (Map или Fields+Join).

// --- Блок F. Регистры и Unicode ---

// 36. Применяй strings.ToLower/ToUpper на смешанных скриптах (латиница+кириллица).
// 37. Используй unicode.ToUpper/ToLower для ручного прохода по рунам.
// 38. Проверь unicode.IsDigit, IsLetter, IsSpace, IsPunct на разных примерах.
// 39. Используй unicode.SimpleFold для case-insensitive поиска символа.
// 40. Отфильтруй из строки только буквы (unicode.IsLetter), сохрани порядок.

// --- Блок G. bytes.* зеркала строковых функций ---

// 41. Повтори задания 21–29, но для []byte с bytes.Contains/Index/Split/Trim*.
// 42. Сравни bytes.Equal и bytes.EqualFold на примерах с ASCII и не-ASCII.
// 43. Сделай bytes.Repeat([]byte("-"), 10) и собери рамку вокруг текста.
// 44. Напиши BytesReplaceInPlace: используя один буфер, заменяй подстроки (на уровне байт).
// 45. Реализуй безопасную конкатенацию многих []byte: единый make и copy.

// --- Блок H. Builder/Buffer и форматирование ---

// 46. Собери длинную строку через конкатенацию + в цикле; измерь время/выделения (testing.Benchmark).
// 47. То же через strings.Builder: WriteString/WriteRune — сравни по выделениям.
// 48. То же через bytes.Buffer: WriteString/WriteRune.
// 49. Сформируй строку через fmt.Fprintf(&builder, "id=%d name=%s", ...).
// 50. Используй fmt.Append / Appendf / Appendln для построения []byte без fmt.Sprintf.

// --- Блок I. Readers/Writers поверх строк/байтов ---

// 51. Прочитай строку через strings.NewReader и io.ReadAll.
// 52. Прочитай первые N байт через io.LimitReader(strings.NewReader(...), N).
// 53. Перемотай strings.Reader: ReadAt и Seek, выведи позицию.
// 54. Используй bytes.NewReader для []byte: ReadRune и UnreadRune (через bufio.NewReader).
// 55. Скопируй из Reader в файл через io.Copy (строка -> файл).

// --- Блок J. Конвертации и strconv ---

// 56. Преобразуй числа в строку: strconv.Itoa, FormatInt/FormatFloat с заданной точностью.
// 57. Распарси строку в число: Atoi, ParseInt (основание 16), ParseFloat.
// 58. Преобразуй []rune в []byte и обратно корректно (через промежуточную строку и напрямую через utf8.EncodeRune).
// 59. Закодируй/декодируй hex и base64 из/в []byte (encoding/hex, encoding/base64).
// 60. Экранируй строку/раскодируй литерал: strconv.Quote / Unquote.

// --- Блок K. Безопасные подстроки и индексы по рунам ---

// 61. Реализуй функцию SubstringRunes(s string, start, n int) string — по рунам.
// 62. Реализуй IndexNthRune(s string, n int) int — верни байтовый индекс n-й руны.
// 63. Удали k-ую руну из строки (без порчи UTF-8).
// 64. Разверни строку по рунам (corretto: учитывай суррогаты? — в Go нет суррогатных пар, но руны могут быть >1 байта).
// 65. Обрежь строку до maxRunes рун, добавив многоточие "…" при усечении.

// --- Блок L. Валидность и очистка UTF-8 ---

// 66. Заменяй невалидные последовательности на '�' (RuneError) при ручном декодировании.
// 67. Реализуй SanitizeToValidUTF8([]byte) []byte — оставь только валидные руны, остальное замени '?'.
// 68. Проверь utf8.FullRune / utf8.RuneStart в потоке байтов (стриминг).
// 69. Считай строки из файла в неизвестной кодировке: отфильтруй невалидные (просто пропусти/замени).

// --- Блок M. Сегментация (гранулярность) и ограничения модели ---

// 70. Подсчитай «символы» как руны и сравни с «графемами» для строки "🇺🇦👨‍👩‍👧‍👦" (обсуди, почему руны != графемы; stdlib не сегментирует).
// 71. (Опционально) Используй внешнюю библиотеку для нормализации/графем — отметь в комментарии, что это вне stdlib.

// --- Блок N. Практикум «из жизни» ---

// 72. Преврати CSV-строку в заголовки Go-полей: "user id,name" -> "UserID", "Name" (Builder + Map).
// 73. Найди и замени все email в тексте на "***@***" (bytes/strings + простая эвристика или regexp).
// 74. Подсчитай топ-10 слов (unicode-слова), выведи частоты по убыванию (runes + map + сортировка).
// 75. Реализуй обрезку по байтам с сохранением целостности UTF-8: не разрезай внутри руны.
// 76. Реализуй нормализацию пробелов и пунктуации: убрать двойные, обрезать вокруг знаков.
// 77. Сконвертируй массив рун в JSON-массив кодовых точек и обратно.
// 78. Сделай фильтр: оставь только ASCII (байт < 128), остальное отбрось.
// 79. Сравни память/время: конкатенации '+', Builder и Buffer на 10000 кусочков.
// 80. Реализуй безопасный форматтер: в template "Hello, {name}" подставь значения из map[string]string без regexp.

// --- Блок O. Тесты и пограничные случаи ---

// 81. Табличный тест для SubstringRunes: пустая строка, ASCII, не-ASCII, start за пределами, n > длины.
// 82. Тест для EqualFold на немецком "ß" vs "SS" и турецком "i/İ" (зафиксируй тонкости).
// 83. Фазз-тест на SanitizeToValidUTF8: случайные байты -> всегда валидная строка.
// 84. Бенчмарк для поиска подстроки: Index vs IndexRune vs ручной DecodeRune.
// 85. Property-тест: string -> []rune -> string сохраняет эквивалентность для валидного UTF-8.
